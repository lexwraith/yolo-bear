number main()
{
    number i = 0;

    // let's make our own array of strings
    char states[4] = {
        "California", "Oregon",
        "Washington", "Texas"
    };
    int num_states = 4;

    for(i = 1; i <= num_states; i++) {
        print("state %d: %s\n", i, states[i]);
    }

    return 0;
}