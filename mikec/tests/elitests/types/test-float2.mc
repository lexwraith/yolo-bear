//test basic float arithmetic 

int main(){

    float a, b, x, y, z;
    a = 1;
    b = 1.5;

    //addition
    x = a + b;
    y = a + .5;
    printf("2.5: %f, 1.5: %f\n", x, y);

    //subtraction
    x = b - a;
    y = a - b;
        
    printf("0.5: %f, -0.5: %f\n", x, y);

    //multiplication
    c = a*b;
    
