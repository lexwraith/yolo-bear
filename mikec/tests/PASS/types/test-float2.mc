int main(){

    float a, b, x, y, z;
    a = 1;
    b = 1.5;

    x = a + b;
    y = a + .5;
    printf("2.5: %f, 1.5: %f\n", x, y);

    x = b - a;
    y = a - b;
        
    printf("0.5: %f, -0.5: %f\n", x, y);

    c = a*b;
    
