float main() {

float array[10], c, d;

print(Enter the number of elements:\n");
scanf("%d", n); //how are we doing this?

print("Enter %d numbers:\n", n);
scanf("%d", &n); //how do

for (c = 0; c<n; c++)
	scanf("%d", &array[c]); //same

for (c = 0; c < n; c++) {
	for (d = 0; d < n - c - 1; d++) {
		if (array[d] > array[d+1]) {
			array[d], array[d+1] = array[d+1], array[d];
		}
	}
}

for (c = 0; c < n; c++) 
	printf("%d\n", array[c]);
	
return 0;
}
